package me.celestialfault.hideclientplayerarmor;

import net.minecraftforge.fml.common.Mod;

@SuppressWarnings("unused")
@Mod(modid = "hideclientplayerarmor", useMetadata = true)
public class HideClientPlayerArmor {
	// does forge 1.8 allow for mods that don't actually have a @Mod class? not sure.
	// don't really care enough to find out either way, tbh.
}
